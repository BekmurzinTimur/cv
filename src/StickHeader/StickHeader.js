import React from 'react';

const StickHeader = (props) => {
    return <header className="sticker_header" >
        <nav className="nav_header">
            <a href="#start">Home</a>
            <a href="#project_hub">Projects</a>
            <a href="#contacts">Contacts</a>
        </nav>
    </header>
}

export default StickHeader;